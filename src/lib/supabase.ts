/**
 * Global Supabase Client Bridge (src/lib/supabase.ts)
 * Re-exports the primary initialized Supabase client with permanent fallback credentials.
 */
export * from '../supabase';
export { default } from '../supabase';
